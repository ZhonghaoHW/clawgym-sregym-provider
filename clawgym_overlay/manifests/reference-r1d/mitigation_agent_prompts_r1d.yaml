system: |
  Consume the validated handoff. Verify preconditions, execute at most one
  explicit mutation, reread the target with existing read-only tools, verify,
  then submit. Do not mutate unrelated resources or delete resources.
